---
phase: 01-security-and-collaboration-contract-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/capabilities.json
  - tools/capability_manifest.py
  - tools/preflight-checks.py
  - tools/run-checks.sh
  - tools/verify-local.sh
  - tests/tools/test-capability-manifest.py
  - .github/workflows/checks.yml
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "本地与 CI 动态发现并验证同一 capability 集合，新增 matrix/workflow 不要求修改硬编码计数。"
    - "未执行、无证据或 required 依赖缺失绝不报告 PASS。"
    - "phase gate 在同一 source/image tuple、全部开关关闭时同时运行 smoke.py 与 baseline.py。"
  artifacts:
    - {path: config/capabilities.json, provides: "canonical capability declarations"}
    - {path: tools/capability_manifest.py, provides: "dynamic parity and four-state result classifier"}
    - {path: tests/tools/test-capability-manifest.py, provides: "Wave 0 contract tests"}
  key_links:
    - {from: tools/verify-local.sh, to: config/capabilities.json, via: "manifest CLI"}
    - {from: .github/workflows/checks.yml, to: tools/preflight-checks.py, via: "blocking English-named CI step"}
---

<objective>
建立真实、动态、可扩展的 capability gate，并先用红测锁定 PASS/SKIP/FAIL/NOT RUN 语义。

Purpose: 后续安全合同必须依赖一个不会漏检或假绿的控制面。
Output: 动态 manifest parity、共享本地/CI runner、disabled CE 双基线 gate。
</objective>

<execution_context>
@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/bobo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-and-collaboration-contract-closure/01-CONTEXT.md
@.planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 先建立动态 parity 与状态真值红测</name>
  <files>config/capabilities.json, tests/tools/test-capability-manifest.py</files>
  <read_first>
    - AGENTS.md
    - tools/preflight-checks.py
    - tools/verify-local.sh
    - tests/e2e/smoke.py
    - tests/e2e/baseline.py
    - .github/workflows/external_sources-e2e.yml
  </read_first>
  <action>创建 manifest schema 与纯 Python 红测。manifest 顶层增加 machine-readable `workflow_classifications`，每个 `.github/workflows/*-e2e.yml` 必须且只能声明 `{kind: capability, capability_id}` 或 `{kind: non-capability, reason}`；将 `build-and-e2e.yml` 明确分类为 non-capability orchestration workflow。测试扫描全部 `*-e2e.yml`，拒绝未分类/重复/未知 kind，只把 `kind=capability` 的 workflow IDs 与 capability declarations 和 `tests/e2e/*_matrix.py` 比较；不得断言固定数量。明确验证 `.github/workflows/external_sources-e2e.yml` 为 capability。锁定 PASS 仅允许 executed+assertions_passed+evidence_id，required 缺失为 FAIL，optional 且有 reason 为 SKIP，缺 record 为 NOT RUN。另定义 phase gate 必须记录一个 tuple identity，并在该 tuple、全部 `CF_ENABLE_*` 关闭时顺序运行 `smoke.py` 与 `baseline.py`。</action>
  <acceptance_criteria>新增 `*-e2e.yml` 若未分类会失败；build-and-e2e 明确 non-capability 且不参与 capability parity；external_sources 路径和 capability 标记正确；五类结果真值和同 tuple 双基线都有失败断言；实现模块缺失时测试红而非语法错误。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/tools/test-capability-manifest.py &amp;&amp; ! python3 tests/tools/test-capability-manifest.py &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>Wave 0 gate contract 可执行且先于实现失败。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 manifest、runner 与 CI gate</name>
  <files>tools/capability_manifest.py, tools/preflight-checks.py, tools/run-checks.sh, tools/verify-local.sh, .github/workflows/checks.yml</files>
  <read_first>
    - AGENTS.md
    - config/capabilities.json
    - tests/tools/test-capability-manifest.py
    - tools/run-checks.sh
    - .github/workflows/checks.yml
  </read_first>
  <action>实现无第三方依赖的 manifest validate/list/classify/aggregate CLI；preflight 先验证每个 `*-e2e.yml` 的 `workflow_classifications`，再仅对 `kind=capability` 动态比较 manifest capability、matrix、workflow，不维护 capability 数量，明确忽略已分类的 build-and-e2e non-capability workflow。runner 只输出四态且 required FAIL/NOT RUN 非零。为 phase gate 捕获同一 source SHA/image digest tuple，清空全部功能开关后依次运行 `python3 tests/e2e/smoke.py` 与 `python3 tests/e2e/baseline.py`，任一失败即阻断。CI 步骤名统一英文（例如 `Validate capability manifest`、`Run disabled CE baselines`），无 continue-on-error。</action>
  <acceptance_criteria>动态 parity 绿；本地/CI 使用同一 manifest；双基线证据携同一 tuple；所有开关关闭保持原生 CE；ASVS L1 high finding 或 required missing 均阻断。</acceptance_criteria>
  <verify><automated>python3 tests/tools/test-capability-manifest.py &amp;&amp; bash -n tools/run-checks.sh tools/verify-local.sh &amp;&amp; ruby -e 'require "yaml"; YAML.load_file(".github/workflows/checks.yml", aliases: true)' &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>真实 gate 已实现并成为所有后续计划的依赖。</done>
</task>
</tasks>

<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| manifest 漏项/篡改 | Tampering | high | mitigate | 动态文件集合 parity；未关闭即阻断 |
| skipped 假报 PASS | Repudiation | high | mitigate | 四态真值、证据 ID、非零聚合 |
| disabled baseline 漂移 | Elevation of Privilege | high | mitigate | 同 tuple 运行 smoke+baseline |
</threat_model>

<verification>任务使用 focused tests；计划 gate 运行 manifest suite、preflight 和同 tuple disabled 双基线。</verification>
<success_criteria>动态 parity、四态结果和 disabled CE 双基线均有自动证据，high severity 为阻断项。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-01-SUMMARY.md`</output>
