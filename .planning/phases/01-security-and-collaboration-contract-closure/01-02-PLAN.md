---
phase: 01-security-and-collaboration-contract-closure
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/acl-cases.json
  - ../cloudfile-hub/cloudfile_ext/acl/tests/test_revision.py
  - ../cloudfile-hub/cloudfile_ext/search/tests/test_authorization.py
  - ../cloudfile-server/fileserver/cf_ext_test.go
  - ../cloudfile-server/tests/cf-acl/gen-cases.py
  - ../cloudfile-server/tests/cf-acl/test-cf-acl.c
  - tests/e2e/acl_matrix.py
  - tests/e2e/search_matrix.py
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "stock/disabled unsupported 与 active+RPC-missing unavailable 是不同状态。"
    - "active authority 的 unavailable/malformed/stale 状态在内容边界 fail closed。"
    - "搜索隐藏项不影响可观察 name/path/snippet/total/cache。"
  artifacts:
    - {path: docs/acl-cases.json, provides: "cross-repo ACL authority states"}
    - {path: tests/e2e/search_matrix.py, provides: "backend-independent leakage tests"}
  key_links:
    - {from: docs/acl-cases.json, to: ../cloudfile-server/fileserver/cf_ext_test.go, via: "shared state fixtures"}
---

<objective>先定义 ACL authority 与搜索泄漏的跨仓红测，使实现不能混淆 disabled 和 active outage。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/REQUIREMENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-CONTEXT.md @.planning/phases/01-security-and-collaboration-contract-closure/01-RESEARCH.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 锁定 authority 状态与 revision 红测</name>
  <files>docs/acl-cases.json, ../cloudfile-hub/cloudfile_ext/acl/tests/test_revision.py, ../cloudfile-server/fileserver/cf_ext_test.go, ../cloudfile-server/tests/cf-acl/gen-cases.py, ../cloudfile-server/tests/cf-acl/test-cf-acl.c</files>
  <read_first>
    - AGENTS.md
    - docs/acl-cases.json
    - ../cloudfile-hub/cloudfile_ext/acl/service.py
    - ../cloudfile-server/common/cf-acl.c
    - ../cloudfile-server/fileserver/cf_ext.go
  </read_first>
  <action>扩展共享 fixture 为 `unsupported-stock`, `inactive-disabled`, `active-valid`, `active-unavailable`, `active-malformed`, `active-stale`。只有前两者允许 native CE pass-through；当 feature active 但 RPC 未注册/超时/无 revision 时必须 deny。Python/C/Go tests 直接消费同一 fixture，覆盖启停、重启、revision 单调和 active RPC-missing。</action>
  <acceptance_criteria>disabled/stock pass-through 与 active missing authority deny 由不同 case 精确断言；未知 state 拒绝；focused tests 在实现前至少一项红。</acceptance_criteria>
  <verify><automated>python3 -m json.tool docs/acl-cases.json &gt;/dev/null &amp;&amp; python3 -m py_compile ../cloudfile-server/tests/cf-acl/gen-cases.py &amp;&amp; ! (cd ../cloudfile-server/fileserver &amp;&amp; go test -count=1 -run 'Test.*Authority' .) &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>ACL authority 安全真值先于任何实现存在。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定两搜索后端的可观察泄漏红测</name>
  <files>../cloudfile-hub/cloudfile_ext/search/tests/test_authorization.py, tests/e2e/acl_matrix.py, tests/e2e/search_matrix.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/01-security-and-collaboration-contract-closure/01-UI-SPEC.md
    - ../cloudfile-hub/seahub/api2/views.py
    - ../cloudfile-hub/cloudfile_ext/search/backends/meilisearch.py
    - tests/e2e/search_matrix.py
  </read_first>
  <action>为 SeaSearch 与 Meili 建立相同红测：正确性优先分批 over-fetch，授权前不得形成 snippet/total/cache；覆盖全隐藏页、跨页补满、精确 total/has_more、缓存命中后撤权、provider restart。响应 body 中不得出现隐藏 name/path/snippet。</action>
  <acceptance_criteria>两 backend 使用同一 case 表；total/pagination/cache/revoke 均为精确断言；实现前 focused Hub test 红。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/acl_matrix.py tests/e2e/search_matrix.py &amp;&amp; ! (cd ../cloudfile-hub &amp;&amp; python3 -m pytest cloudfile_ext/search/tests/test_authorization.py -q) &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>SEC-01/02 的负例与禁泄漏边界已由 Wave 0 tests 固定。</done>
</task>
</tasks>

<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| active outage 被当 disabled | Elevation of Privilege | high | mitigate | 独立状态与 RPC-missing deny test |
| search metadata 泄漏 | Information Disclosure | high | mitigate | authorization-before-observability red tests |
</threat_model>
<verification>仅运行 fixture parser、focused Hub/Go/C tests；完整矩阵留到 01-05 gate。</verification>
<success_criteria>所有 ACL/搜索安全行为先红，且 native disabled 行为有正向合同。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-02-SUMMARY.md`</output>
