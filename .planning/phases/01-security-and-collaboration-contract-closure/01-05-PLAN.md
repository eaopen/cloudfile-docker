---
phase: 01-security-and-collaboration-contract-closure
plan: "05"
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - tests/e2e/acl_matrix.py
  - tests/e2e/search_matrix.py
  - .github/workflows/acl-e2e.yml
  - .github/workflows/search-e2e.yml
  - config/capabilities.json
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "两搜索后端和 Server ACL 在真实服务矩阵中通过正负例。"
    - "disabled matrix 证明原生 CE，active RPC-missing matrix 证明 deny。"
  artifacts:
    - {path: tests/e2e/search_matrix.py, provides: "SeaSearch/Meili gate"}
    - {path: .github/workflows/acl-e2e.yml, provides: "blocking ACL CI"}
  key_links:
    - {from: config/capabilities.json, to: .github/workflows/search-e2e.yml, via: "dynamic gate declaration"}
---

<objective>把 ACL authority 与搜索授权实现接入真实矩阵和 truthful gate。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-03-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-04-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 接入 ACL/search integration matrices</name>
  <files>tests/e2e/acl_matrix.py, tests/e2e/search_matrix.py, config/capabilities.json</files>
  <read_first>
    - AGENTS.md
    - config/capabilities.json
    - tests/e2e/acl_matrix.py
    - tests/e2e/search_matrix.py
    - .planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md
  </read_first>
  <action>完成 shared cases 的容器编排：stock/disabled native pass、active valid allow/deny、active RPC service stopped/unregistered/malformed/stale deny；SeaSearch/Meili 精确 total/page/snippet/cache/revoke/restart。manifest 只声明 matrix/workflow 元数据，parity 由动态发现实现，不写固定 capability 数量。</action>
  <acceptance_criteria>每个 state 有独立证据；active missing authority 无响应 body；两搜索后端精确结果相同；manifest parity 绿。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/acl_matrix.py tests/e2e/search_matrix.py &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>ACL/search 矩阵可由 gate 发现并有完整负例。</done>
</task>

<task type="auto">
  <name>Task 2: 配置 blocking CI integration gates</name>
  <files>.github/workflows/acl-e2e.yml, .github/workflows/search-e2e.yml</files>
  <read_first>
    - AGENTS.md
    - .github/workflows/acl-e2e.yml
    - .github/workflows/search-e2e.yml
    - tools/verify-local.sh
  </read_first>
  <action>工作流步骤名全部英文；同一 checkout tuple 运行 ACL/search matrices，关闭扩展的 native phase 也必须执行。删除 continue-on-error；required backend/service 缺失为 FAIL。完整矩阵只在本计划 gate/CI 运行，task sampling 保持 py_compile+preflight。</action>
  <acceptance_criteria>YAML 可解析；无绕过安全矩阵的条件；工作流仅使用英文 step names；ASVS L1 high failure 阻断。</acceptance_criteria>
  <verify><automated>ruby -e 'require "yaml"; %w[acl-e2e.yml search-e2e.yml].each{|f| YAML.load_file(".github/workflows/#{f}", aliases: true)}' &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>ACL/search security gates 可执行且不能假绿。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| integration negative skipped | Repudiation | high | mitigate | required matrices, no continue-on-error |
</threat_model>
<verification>计划 gate：`./tools/verify-local.sh cap acl` 与 `./tools/verify-local.sh cap search`；两者完整矩阵在此处而非每任务执行。</verification>
<success_criteria>ACL/search 全矩阵和 disabled/active-outage 对照通过。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-05-SUMMARY.md`</output>
