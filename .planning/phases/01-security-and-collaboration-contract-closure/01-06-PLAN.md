---
phase: 01-security-and-collaboration-contract-closure
plan: "06"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/fileop-cases.json
  - tests/e2e/fileop_matrix.py
  - ../cloudfile-server/tests/cf-fileop/gen-cases.py
  - ../cloudfile-server/tests/cf-fileop/test-cf-fileop.c
  - ../cloudfile-server/tests/cf-fileop/run.sh
  - ../cloudfile-server/fileserver/cf_fileop_test.go
  - ../cloudfile-server/fileserver/fileop_test.go
autonomous: true
requirements: [FILEOP-01, LOCK-01]
must_haves:
  truths:
    - "一个 operation_id 只有一个 PREPARE 和一个终态。"
    - "expected_commit_id 在 JSON/C/Go/Python/SQL/fixtures 使用同一拼写与语义。"
    - "stale/missing generation 或 expected versions 拒绝 mutation/release。"
  artifacts:
    - {path: docs/fileop-cases.json, provides: "canonical operation and fence fixtures"}
    - {path: tests/e2e/fileop_matrix.py, provides: "cross-path lifecycle matrix"}
  key_links:
    - {from: docs/fileop-cases.json, to: ../cloudfile-server/tests/cf-fileop/gen-cases.py, via: "shared fixture generation"}
---

<objective>先锁定操作身份、唯一终态与统一 expected_commit_id fence 的红测。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/REQUIREMENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-CONTEXT.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立 canonical operation/fence fixture</name>
  <files>docs/fileop-cases.json, ../cloudfile-server/tests/cf-fileop/gen-cases.py, ../cloudfile-server/tests/cf-fileop/test-cf-fileop.c, ../cloudfile-server/tests/cf-fileop/run.sh</files>
  <read_first>
    - AGENTS.md
    - ../cloudfile-server/common/cf-fileop.h
    - ../cloudfile-server/common/cf-lock.h
    - ../cloudfile-server/tests/cf-fileop/test-cf-fileop.c
    - ../cloudfile-server/tests/cf-fileop/run.sh
  </read_first>
  <action>定义唯一 wire/storage 字段 `expected_commit_id`，并明确映射到同名 C struct member、Go JSON tag、Python RPC parameter、SQL column；禁止 `expect_commit_id`/`base_commit_id` 在 fence 接口中作为别名。fixture 明确两类失败：PREPARE/provider 在 native mutation 前拒绝 → ABORTED；native write 已成功后的 observer/report failure → native COMMITTED，另记 retry/audit work item，且绝不产生第二 terminal。覆盖 stable operation_id、batch delete one scope、duplicate terminal、missing/stale generation/file/commit。扩展 `run.sh --expect-red PATTERN`：先完整 compile/link 作为 build guard，compile/link 失败立即失败；再实际执行 C harness，只有 harness 非零且输出匹配新增命名断言（例如 `post_write_observer_committed_once`）才返回成功，避免把无关编译错误当预期红。</action>
  <acceptance_criteria>fixture schema 只有 expected_commit_id；generator 拒绝别名；pre-mutation rejection 只 ABORTED；post-write observer/report failure 只 COMMITTED 并产生 retry/audit、不产生第二终态；`run.sh --expect-red` 能区分 build failure 与指定行为红测。</acceptance_criteria>
  <verify><automated>python3 -m json.tool docs/fileop-cases.json &gt;/dev/null &amp;&amp; python3 -m py_compile ../cloudfile-server/tests/cf-fileop/gen-cases.py &amp;&amp; bash -n ../cloudfile-server/tests/cf-fileop/run.sh &amp;&amp; (cd ../cloudfile-server &amp;&amp; ./tests/cf-fileop/run.sh --expect-red 'post_write_observer_committed_once|expected_commit_id_contract') &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>跨语言字段与生命周期合同在实现前固定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 Go/E2E 写路径红测</name>
  <files>../cloudfile-server/fileserver/cf_fileop_test.go, ../cloudfile-server/fileserver/fileop_test.go, tests/e2e/fileop_matrix.py</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/fileserver/cf_fileop.go
    - ../cloudfile-server/fileserver/fileop.go
    - tests/e2e/fileop_matrix.py
  </read_first>
  <action>让 Go tests 消费同一 cases，断言 JSON key `expected_commit_id`、one scope/one terminal。扩展 E2E case definitions 覆盖 API/fileserver/sync/WebDAV/batch delete、retry、pre-mutation provider rejection→ABORTED、post-native-write observer/report failure→COMMITTED+retry/audit 且无第二终态、same-owner stale generation；实现前仅跑 focused Go test 并要求红。</action>
  <acceptance_criteria>所有写入口有 case；disabled provider 零 lifecycle facts 且 native write 成功；post-write observer failure 不得 ABORT；新 focused test 因实现缺失失败。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/fileop_matrix.py &amp;&amp; ! (cd ../cloudfile-server/fileserver &amp;&amp; go test -count=1 -run 'Test.*Operation|Test.*Fence' .) &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>FILEOP/LOCK Wave 0 tests 先红且不运行完整矩阵。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| operation identity substitution | Spoofing | high | mitigate | stable ID cross-path tests |
| commit fence field drift | Tampering | high | mitigate | exact expected_commit_id fixture mapping |
</threat_model>
<verification>仅 JSON/generator/focused Go/C red tests；矩阵留到 01-08。</verification>
<success_criteria>跨语言合同唯一且所有 mutation/release 负例有 Wave 0 coverage。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-06-SUMMARY.md`</output>
