---
phase: 01-security-and-collaboration-contract-closure
plan: "07"
type: execute
wave: 4
depends_on: ["01-04", "01-06"]
files_modified:
  - ../cloudfile-server/scripts/sql/mysql/cloudfile.sql
  - ../cloudfile-server/common/cf-fileop.h
  - ../cloudfile-server/common/cf-fileop.c
  - ../cloudfile-server/common/cf-lock.h
  - ../cloudfile-server/common/cf-lock.c
  - ../cloudfile-server/server/repo-op.c
  - ../cloudfile-server/common/rpc-service.c
autonomous: true
requirements: [FILEOP-01, LOCK-01]
must_haves:
  truths:
    - "每条写路径建立一个共享 operation scope 并只终结一次。"
    - "mutation 与 release 强制 generation、expected_file_id、expected_commit_id。"
  artifacts:
    - {path: ../cloudfile-server/common/cf-fileop.c, provides: "native operation scope"}
    - {path: ../cloudfile-server/common/rpc-service.c, provides: "native fenced RPC contract"}
  key_links:
    - {from: ../cloudfile-server/server/repo-op.c, to: ../cloudfile-server/common/cf-fileop.c, via: "one native operation scope"}
---

<objective>实现 Server native C/RPC 的单一 operation scope、唯一终态与 generation/version fence。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-06-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 C native operation scope 与唯一终态</name>
  <files>../cloudfile-server/scripts/sql/mysql/cloudfile.sql, ../cloudfile-server/common/cf-fileop.h, ../cloudfile-server/common/cf-fileop.c, ../cloudfile-server/server/repo-op.c, ../cloudfile-server/common/rpc-service.c</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/common/cf-fileop.h
    - ../cloudfile-server/server/repo-op.c
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>引入可传递的 `CfFileOp` scope；外层入口生成/接受 operation_id，batch delete 只建一次 PREPARE，所有退出统一 finalize CAS。C struct、SQL、JSON 全部命名 `expected_commit_id`。pre-mutation rejection 终结为 ABORTED；post-native-write observer/report failure 保持 COMMITTED、登记 retry/audit 且 finalize CAS 禁止第二终态。RPC 公开完整 native fence contract，adapter wiring 留给 Plan 17。</action>
  <acceptance_criteria>没有独立宏各自产生 ID；同 op 只有单终态；post-write observer failure 只有 COMMITTED；字段拼写全局一致。</acceptance_criteria>
  <verify><automated>cd ../cloudfile-server &amp;&amp; ./tests/cf-fileop/run.sh &amp;&amp; cd ../cloudfile-docker &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>C native lifecycle 与 lock fence 实现并通过 focused tests。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 native lock generation/version fence</name>
  <files>../cloudfile-server/common/cf-lock.h, ../cloudfile-server/common/cf-lock.c, ../cloudfile-server/common/rpc-service.c</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/common/cf-lock.h
    - ../cloudfile-server/common/cf-lock.c
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>live lease 存在时 owner 相同也必须 exact generation；expected_file_id/expected_commit_id 不匹配返回 version mismatch。release SQL 永远含 generation。注册 C RPC `cf_put_file_fenced` 的 9 参数签名并只进入 existing-file update；Go/Python binding 在 Plan 17 实现。</action>
  <acceptance_criteria>stale/missing generation、file、commit 不写不释放；RPC 参数与 fixture 精确一致；C focused tests 绿。</acceptance_criteria>
  <verify><automated>cd ../cloudfile-server &amp;&amp; ./tests/cf-fileop/run.sh &amp;&amp; cd ../cloudfile-docker &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>native C/RPC lifecycle 与 fence 已实现，可供 adapter 绑定。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| duplicate/missing terminal | Repudiation | high | mitigate | finalize CAS and pairing tests |
| stale same-owner write | Tampering | high | mitigate | generation+file+commit fence |
</threat_model>
<verification>focused C harness+preflight；Go/Python integration 由 01-17，完整 matrix 由 01-08。</verification>
<success_criteria>native lifecycle 只终结一次，所有 stale native fence 安全拒绝。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-07-SUMMARY.md`</output>
