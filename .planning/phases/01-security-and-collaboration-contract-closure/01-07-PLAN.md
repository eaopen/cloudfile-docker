---
phase: 01-security-and-collaboration-contract-closure
plan: "07"
type: execute
wave: 4
depends_on: ["01-04", "01-06"]
files_modified:
  - ../cloudfile-server/scripts/sql/mysql/cloudfile.sql
  - ../cloudfile-server/common/cf-fileop.h
  - ../cloudfile-server/common/cf-fileop.c
  - ../cloudfile-server/common/cf-lock.h
  - ../cloudfile-server/common/cf-lock.c
  - ../cloudfile-server/server/repo-op.c
  - ../cloudfile-server/common/rpc-service.c
  - ../cloudfile-server/fileserver/cf_fileop.go
  - ../cloudfile-server/fileserver/cf_ext.go
  - ../cloudfile-server/fileserver/fileop.go
  - ../cloudfile-server/python/seaserv/api.py
autonomous: true
requirements: [FILEOP-01, LOCK-01]
must_haves:
  truths:
    - "每条写路径建立一个共享 operation scope 并只终结一次。"
    - "mutation 与 release 强制 generation、expected_file_id、expected_commit_id。"
  artifacts:
    - {path: ../cloudfile-server/common/cf-fileop.c, provides: "native operation scope"}
    - {path: ../cloudfile-server/python/seaserv/api.py, provides: "fenced existing-file RPC adapter"}
  key_links:
    - {from: ../cloudfile-server/fileserver/fileop.go, to: ../cloudfile-server/common/rpc-service.c, via: "one operation identity"}
---

<objective>实现 Server C/Go/Python 的单一 operation scope 与 generation/version fence。</objective>
<scope_note>本计划虽跨 11 个文件，但它们共同构成一个 native mutation transaction boundary（C scope/lock、RPC、Go adapter、Python binding）；不含 E2E gate、Hub workflow 或 UI，属于 bounded cohesive scope。</scope_note>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-06-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 实现 C native operation scope 与 fence</name>
  <files>../cloudfile-server/scripts/sql/mysql/cloudfile.sql, ../cloudfile-server/common/cf-fileop.h, ../cloudfile-server/common/cf-fileop.c, ../cloudfile-server/common/cf-lock.h, ../cloudfile-server/common/cf-lock.c, ../cloudfile-server/server/repo-op.c, ../cloudfile-server/common/rpc-service.c</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/common/cf-fileop.h
    - ../cloudfile-server/common/cf-lock.c
    - ../cloudfile-server/server/repo-op.c
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>引入可传递的 `CfFileOp` scope；外层入口生成/接受 operation_id，batch delete 只建一次 PREPARE，所有退出统一 finalize CAS。C struct、SQL、JSON 全部命名 `expected_commit_id`。live lease 存在时 owner 相同也必须 exact generation；expected_file_id/expected_commit_id 不匹配返回 version mismatch。release SQL 永远含 generation。</action>
  <acceptance_criteria>没有独立宏各自产生 ID；同 op 只有单终态；字段拼写全局一致；stale/missing fence 不写不释放。</acceptance_criteria>
  <verify><automated>cd ../cloudfile-server &amp;&amp; ./tests/cf-fileop/run.sh &amp;&amp; cd ../cloudfile-docker &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>C native lifecycle 与 lock fence 实现并通过 focused tests。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 贯通 Go 与 Python fenced update</name>
  <files>../cloudfile-server/fileserver/cf_fileop.go, ../cloudfile-server/fileserver/cf_ext.go, ../cloudfile-server/fileserver/fileop.go, ../cloudfile-server/python/seaserv/api.py</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/fileserver/cf_fileop_test.go
    - ../cloudfile-server/python/seaserv/api.py
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>Go request/struct JSON tag 与 Python signature 都使用 `expected_commit_id`。注册 `cf_put_file_fenced(repo_id,tmp,parent,name,user,operation_id,generation,expected_file_id,expected_commit_id)`，只调用 existing-file update，冲突映射 409/CF_ERR_VERSION_MISMATCH。Go 各入口把一个 scope 传到底，不能重新生成 operation_id。</action>
  <acceptance_criteria>JSON/C/Go/Python/SQL/fixture 精确同名；Python 不调用 post/create；focused Go tests 绿。</acceptance_criteria>
  <verify><automated>cd ../cloudfile-server/fileserver &amp;&amp; go test -count=1 -run 'Test.*Operation|Test.*Fence' . &amp;&amp; cd ../../cloudfile-docker &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>跨语言 fenced existing-file update 可供 local/office 安全复用。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| duplicate/missing terminal | Repudiation | high | mitigate | finalize CAS and pairing tests |
| stale same-owner write | Tampering | high | mitigate | generation+file+commit fence |
</threat_model>
<verification>focused C/Go suites+preflight；完整 fileop matrix 由 01-08。</verification>
<success_criteria>所有写路径单 lifecycle，所有 stale fence 安全拒绝。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-07-SUMMARY.md`</output>
