---
phase: 01-security-and-collaboration-contract-closure
plan: "08"
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - docs/fileop-cases.json
  - tests/e2e/fileop_matrix.py
  - .github/workflows/fileop-e2e.yml
  - config/capabilities.json
  - tools/verify-local.sh
autonomous: true
requirements: [FILEOP-01, LOCK-01]
must_haves:
  truths:
    - "所有支持写入口在完整矩阵中只有一条 lifecycle。"
    - "disabled provider 保留 native CE，stale generation/commit 不产生 side effect。"
  artifacts:
    - {path: tests/e2e/fileop_matrix.py, provides: "write-path phase gate"}
    - {path: .github/workflows/fileop-e2e.yml, provides: "blocking CI matrix"}
  key_links:
    - {from: tools/verify-local.sh, to: tests/e2e/fileop_matrix.py, via: "manifest fileop capability"}
---

<objective>把 operation/fence 合同接入 fileop 完整矩阵与 CI gate。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-07-SUMMARY.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 完成跨入口 fileop matrix</name>
  <files>docs/fileop-cases.json, tests/e2e/fileop_matrix.py, config/capabilities.json, tools/verify-local.sh</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - tests/e2e/fileop_matrix.py
    - config/capabilities.json
    - tools/verify-local.sh
  </read_first>
  <action>贯通 browser/API、fileserver/sync、WebDAV、batch delete、retry/observer failure/partial abort。每 case 断言 operation_id、PREPARE 数量、唯一终态、generation 和 expected_commit_id；provider off 零 facts 且 native write 成功。manifest 动态 parity 不含固定数量。</action>
  <acceptance_criteria>全部入口/失败路径有 case；stale 不写不释放；disabled native phase 必跑。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/fileop_matrix.py &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>完整矩阵已准备并由 fileop capability 调用。</done>
</task>

<task type="auto">
  <name>Task 2: 配置 fileop blocking workflow</name>
  <files>.github/workflows/fileop-e2e.yml</files>
  <read_first>
    - AGENTS.md
    - .github/workflows/fileop-e2e.yml
    - .planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md
  </read_first>
  <action>工作流使用英文 step names，运行同一 tuple 的 enabled/disabled matrix；无 continue-on-error，observer/provider required 依赖缺失 FAIL。完整矩阵只在本计划/波次 gate 运行。</action>
  <acceptance_criteria>YAML 解析通过；matrix failure 阻断；英文步骤名；ASVS L1 high finding 阻断。</acceptance_criteria>
  <verify><automated>ruby -e 'require "yaml"; YAML.load_file(".github/workflows/fileop-e2e.yml", aliases: true)' &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>FILEOP/LOCK integration gate 可重复执行。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| write path 未纳入 gate | Tampering | high | mitigate | shared case matrix and required workflow |
</threat_model>
<verification>计划 gate：`./tools/verify-local.sh cap fileop`，完整矩阵仅在此执行。</verification>
<success_criteria>所有写路径和 disabled baseline 通过 fileop gate。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-08-SUMMARY.md`</output>
