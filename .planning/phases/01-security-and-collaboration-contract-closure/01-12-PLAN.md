---
phase: 01-security-and-collaboration-contract-closure
plan: "12"
type: execute
wave: 8
depends_on: ["01-08", "01-11"]
files_modified:
  - tests/e2e/collaboration_matrix.py
  - .github/workflows/collaboration-e2e.yml
  - config/capabilities.json
  - tools/verify-local.sh
autonomous: true
requirements: [LOCAL-01, LOCAL-02]
must_haves:
  truths:
    - "真实 local flow 覆盖 issue→handoff→claim→heartbeat→existing-file save→status。"
    - "真实 Chrome 不可用时结果明确 SKIP，绝不 PASS；contract tests 仍 required。"
  artifacts:
    - {path: tests/e2e/collaboration_matrix.py, provides: "local collaboration gate"}
    - {path: .github/workflows/collaboration-e2e.yml, provides: "blocking collaboration workflow"}
  key_links:
    - {from: config/capabilities.json, to: tests/e2e/collaboration_matrix.py, via: "collaboration capability"}
---

<objective>把 local edit 接入 collaboration gate，并明确真实 Chrome 的环境条件。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-11-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 实现 local collaboration integration phase</name>
  <files>tests/e2e/collaboration_matrix.py, config/capabilities.json, tools/verify-local.sh</files>
  <read_first>
    - AGENTS.md
    - docs/local-session-cases.json
    - tests/e2e/collaboration_matrix.py
    - config/capabilities.json
    - tools/verify-local.sh
  </read_first>
  <action>创建/扩展 collaboration capability 的 local phase：断言 authenticated status transitions、existing file identity、单 version、retry idempotency、heartbeat、conflict/expiry/stale generation。Chrome harness 有 Chrome+extension+native host 时运行真实 handoff；任一环境依赖缺失输出结构化 SKIP+reason，不能 PASS；schema/JS/Agent tests 始终 required。</action>
  <acceptance_criteria>完整 local flow 有 durable outcome 证据；真实 Chrome PASS 或明确 SKIP；manifest 动态 parity 绿。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/collaboration_matrix.py &amp;&amp; bash -n tools/verify-local.sh &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>local collaboration phase 可在环境差异下真实报告。</done>
</task>

<task type="auto">
  <name>Task 2: 配置 collaboration workflow local gate</name>
  <files>.github/workflows/collaboration-e2e.yml</files>
  <read_first>
    - AGENTS.md
    - .github/workflows/collaboration-e2e.yml
    - .planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md
  </read_first>
  <action>使用英文 step names，运行 required contract tests 和 local matrix；上传结构化证据。禁止 continue-on-error；只有真实 Chrome 环境探测可 SKIP browser subcheck，其他 required failure 阻断。</action>
  <acceptance_criteria>YAML 解析通过；英文步骤名；Chrome SKIP 带 reason；high severity failure 阻断。</acceptance_criteria>
  <verify><automated>ruby -e 'require "yaml"; YAML.load_file(".github/workflows/collaboration-e2e.yml", aliases: true)' &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>local collaboration CI gate truthful 且可重复。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| browser handoff 假绿 | Repudiation | high | mitigate | explicit environment SKIP reason |
</threat_model>
<verification>计划 gate：`./tools/verify-local.sh cap collaboration --phase local`；完整 matrix 仅在此运行。</verification>
<success_criteria>local end-to-end flow 通过，Chrome 状态真实且 high findings 阻断。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-12-SUMMARY.md`</output>
