---
phase: 01-security-and-collaboration-contract-closure
plan: "13"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - deploy/compose/.env.example
  - deploy/compose/docker-compose.yml
  - scripts/scripts_14.0/bootstrap.py
  - tools/test-bootstrap-settings.py
  - ../cloudfile-hub/cloudfile_ext/apps.py
  - ../cloudfile-hub/cloudfile_ext/office/__init__.py
  - ../cloudfile-hub/cloudfile_ext/office/tests/test_registry.py
  - ../cloudfile-hub/cloudfile_ext/office/tests/test_callbacks.py
  - ../cloudfile-hub/cloudfile_ext/office/tests/test_download.py
autonomous: true
requirements: [OFFICE-01, OFFICE-02]
must_haves:
  truths:
    - "Office disabled 不注册且保持 CE；enabled 缺同源非空 JWT/API URL 时 startup FAIL。"
    - "callback 在任何 lookup/download/write 前验证 HS256 JWT。"
    - "remote download 对 scheme/host/port/redirect/size 严格失败并清理 tempfile。"
  artifacts:
    - {path: tools/test-bootstrap-settings.py, provides: "startup configuration contract"}
    - {path: ../cloudfile-hub/cloudfile_ext/office/tests/test_download.py, provides: "SSRF and bounded streaming red tests"}
  key_links:
    - {from: deploy/compose/docker-compose.yml, to: scripts/scripts_14.0/bootstrap.py, via: "same-source ONLYOFFICE_JWT_SECRET"}
---

<objective>先锁定并实现 OnlyOffice startup/registration 配置，同时建立 callback 与安全下载红测。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/REQUIREMENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-CONTEXT.md @.planning/phases/01-security-and-collaboration-contract-closure/01-UI-SPEC.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 建立 Office startup/JWT/download 红测</name>
  <files>tools/test-bootstrap-settings.py, ../cloudfile-hub/cloudfile_ext/office/tests/test_registry.py, ../cloudfile-hub/cloudfile_ext/office/tests/test_callbacks.py, ../cloudfile-hub/cloudfile_ext/office/tests/test_download.py</files>
  <read_first>
    - AGENTS.md
    - deploy/compose/docker-compose.yml
    - scripts/scripts_14.0/bootstrap.py
    - ../cloudfile-hub/cloudfile_ext/apps.py
    - ../cloudfile-hub/cloudfile_ext/office/callbacks.py
    - ../cloudfile-hub/seahub/onlyoffice/views.py
  </read_first>
  <action>红测覆盖 disabled no registration、enabled missing secret/API URL startup fail、同源 secret、signed smoke contract、missing/invalid/wrong-alg JWT before side effect。download tests 固定：仅配置的 Document Server normalized scheme+hostname+effective port；拒绝 userinfo/fragment、错误 scheme/host/port；`allow_redirects=False` 且任何 3xx 拒绝；stream 到 bounded tempfile，Content-Length/实际字节超限、timeout/read error 均失败且 finally cleanup。上限由 setting 控制并有安全默认。</action>
  <acceptance_criteria>scheme/host/port/redirect/oversize/partial read/temp cleanup 均有负例；JWT 验证顺序有 mock call assertion；实现前 focused tests 红。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tools/test-bootstrap-settings.py &amp;&amp; ! (cd ../cloudfile-hub &amp;&amp; python3 -m pytest cloudfile_ext/office/tests/test_registry.py cloudfile_ext/office/tests/test_callbacks.py cloudfile_ext/office/tests/test_download.py -q) &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>Office 配置、认证和 SSRF/DoS 边界先由 Wave 0 tests 固定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现同源配置与条件注册</name>
  <files>deploy/compose/.env.example, deploy/compose/docker-compose.yml, scripts/scripts_14.0/bootstrap.py, ../cloudfile-hub/cloudfile_ext/apps.py, ../cloudfile-hub/cloudfile_ext/office/__init__.py</files>
  <read_first>
    - AGENTS.md
    - tools/test-bootstrap-settings.py
    - deploy/compose/.env.example
    - deploy/compose/docker-compose.yml
    - scripts/scripts_14.0/bootstrap.py
    - ../cloudfile-hub/cloudfile_ext/apps.py
  </read_first>
  <action>保留默认 `CF_ENABLE_ONLYOFFICE=false` 与空 secret，不使用 `${VAR:?}`。enabled 时 cloudfile、worker、Document Server 从同一 env 注入 secret；bootstrap trim 后要求 secret/APIJS/internal origin 非空，生成 Hub settings 和安全 download byte limit，且不打印 secret。`apps.ready()` 只在 enabled 时、registry seal 前注册 office；disabled 无 side effect。matching 的运行时证明采用真实 signed callback smoke，不新增 secret fingerprint endpoint。</action>
  <acceptance_criteria>默认 compose render 成功；enabled missing config fail；三服务引用同一 env source；disabled 不注册；signed smoke contract 可执行。</acceptance_criteria>
  <verify><automated>python3 tools/test-bootstrap-settings.py &amp;&amp; (cd deploy/compose &amp;&amp; docker compose --env-file .env.example config --quiet) &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>OnlyOffice 只在安全配置完整时启用，关闭时保留 CE native。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| unsigned callback | Spoofing | critical | mitigate | enabled non-empty HS256 JWT before lookup |
| callback URL SSRF | Information Disclosure | high | mitigate | exact origin, no redirects, bounded stream cleanup |
</threat_model>
<verification>focused settings/JWT/download tests+preflight；完整 Docs matrix 后置。</verification>
<success_criteria>Office config/registration 安全，critical/high tests 关闭。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-13-SUMMARY.md`</output>
