---
phase: 01-security-and-collaboration-contract-closure
plan: "16"
type: execute
wave: 8
depends_on: ["01-12", "01-15"]
files_modified:
  - tests/e2e/collaboration_matrix.py
  - .github/workflows/collaboration-e2e.yml
  - config/capabilities.json
  - tools/verify-local.sh
  - .github/workflows/checks.yml
autonomous: true
requirements: [GATE-01, OFFICE-01, OFFICE-02]
must_haves:
  truths:
    - "真实 Document Server 用同源 secret 完成 signed callback/save/retry/conflict/release。"
    - "最终 phase gate 在同一 tuple、全部开关关闭时运行 smoke.py 与 baseline.py。"
    - "required Docs/JWT 缺失与任何 critical/high security failure 阻断。"
  artifacts:
    - {path: tests/e2e/collaboration_matrix.py, provides: "Office integration matrix"}
    - {path: tools/verify-local.sh, provides: "same-tuple final phase gate"}
  key_links:
    - {from: .github/workflows/collaboration-e2e.yml, to: tests/e2e/collaboration_matrix.py, via: "real Document Server profile"}
---

<objective>完成 Office integration gate 与 Phase 1 同 tuple disabled CE 最终资格验证。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-12-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-15-SUMMARY.md @.planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md</context>
<tasks>
<task type="auto">
  <name>Task 1: 扩展真实 Document Server collaboration matrix</name>
  <files>tests/e2e/collaboration_matrix.py, config/capabilities.json</files>
  <read_first>
    - AGENTS.md
    - tests/e2e/collaboration_matrix.py
    - config/capabilities.json
    - ../cloudfile-hub/cloudfile_ext/office/tests/test_download.py
    - .planning/phases/01-security-and-collaboration-contract-closure/01-VALIDATION.md
  </read_first>
  <action>在同一 collaboration capability 增加 office phase：真实 Docs signed status2/4/6、missing/invalid JWT、duplicate/concurrent/restart retry、conflict/stale、download 3xx/oversize、native update/release failure。逐项断言 callback JSON、durable outcome、file version、lock generation 与 authenticated status API。required Docs/JWT 依赖缺失 FAIL，不是 SKIP。</action>
  <acceptance_criteria>不以 HTTP200 单独判成功；signed smoke 证明同源 secret；所有负例无假成功/重复写；manifest parity 动态绿。</acceptance_criteria>
  <verify><automated>python3 -m py_compile tests/e2e/collaboration_matrix.py &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>Office 完整 integration matrix 已接入 truthful capability。</done>
</task>

<task type="auto">
  <name>Task 2: 配置最终 Phase 1 blocking gate</name>
  <files>.github/workflows/collaboration-e2e.yml, tools/verify-local.sh, .github/workflows/checks.yml</files>
  <read_first>
    - AGENTS.md
    - .github/workflows/collaboration-e2e.yml
    - .github/workflows/checks.yml
    - tools/verify-local.sh
    - tests/e2e/smoke.py
    - tests/e2e/baseline.py
  </read_first>
  <action>工作流 step names 全英文。最终 gate 先记录 source SHA+image digest tuple，运行 ACL/search/fileop/collaboration plan gates；随后在同一 tuple 清空所有 `CF_ENABLE_*`，依次运行 `python3 tests/e2e/smoke.py` 和 `python3 tests/e2e/baseline.py`。任一 required missing、NOT RUN、FAIL、ASVS L1 critical/high finding 非零；真实 Chrome 唯一允许环境性 SKIP并带 reason。</action>
  <acceptance_criteria>两个 baseline 脚本均执行且 evidence tuple 相同；workflow 无 continue-on-error；英文步骤名；Office/local matrices 和 preflight 均 required。</acceptance_criteria>
  <verify><automated>ruby -e 'require "yaml"; %w[collaboration-e2e.yml checks.yml].each{|f| YAML.load_file(".github/workflows/#{f}", aliases: true)}' &amp;&amp; bash -n tools/verify-local.sh &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>Phase 1 最终 gate 可证明 enabled 安全合同与 disabled CE baseline 属于同一 tuple。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| mismatched JWT 假通过 | Spoofing | critical | mitigate | real signed smoke, required Docs |
| rebuilt baseline tuple | Repudiation | high | mitigate | captured identical source/image identity |
| high finding 被忽略 | Elevation of Privilege | high | mitigate | aggregate blocking policy |
</threat_model>
<verification>计划/Phase gate：`./tools/verify-local.sh cap collaboration` 后执行同 tuple disabled `smoke.py` 与 `baseline.py`；完整矩阵只在此 gate。</verification>
<success_criteria>Office 完整矩阵、dynamic parity、同 tuple disabled 双基线全绿，critical/high 全关闭。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-16-SUMMARY.md`</output>
