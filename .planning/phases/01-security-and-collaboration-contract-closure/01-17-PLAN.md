---
phase: 01-security-and-collaboration-contract-closure
plan: "17"
type: execute
wave: 5
depends_on: ["01-07"]
files_modified:
  - ../cloudfile-server/fileserver/cf_fileop.go
  - ../cloudfile-server/fileserver/cf_ext.go
  - ../cloudfile-server/fileserver/fileop.go
  - ../cloudfile-server/python/seaserv/api.py
autonomous: true
requirements: [FILEOP-01, LOCK-01]
must_haves:
  truths:
    - "Go 写入口把一个 operation scope 与完整 fence 传到 native RPC。"
    - "Python binding 只暴露 fenced existing-file update，且统一 expected_commit_id。"
  artifacts:
    - {path: ../cloudfile-server/fileserver/cf_fileop.go, provides: "Go operation adapter"}
    - {path: ../cloudfile-server/python/seaserv/api.py, provides: "Python fenced update binding"}
  key_links:
    - {from: ../cloudfile-server/fileserver/fileop.go, to: ../cloudfile-server/common/rpc-service.c, via: "stable operation_id and fence fields"}
---

<objective>把 Plan 07 的 native lifecycle/fence 合同接入 Go fileserver 与 Python binding。</objective>
<execution_context>@/Users/bobo/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/phases/01-security-and-collaboration-contract-closure/01-07-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: 贯通 Go operation adapter</name>
  <files>../cloudfile-server/fileserver/cf_fileop.go, ../cloudfile-server/fileserver/cf_ext.go, ../cloudfile-server/fileserver/fileop.go</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/fileserver/cf_fileop_test.go
    - ../cloudfile-server/fileserver/fileop_test.go
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>Go request/struct JSON tag 使用 `expected_commit_id`，每个 API/fileserver/sync/WebDAV/batch entry 只创建或接受一个 operation_id，并把 generation、expected_file_id、expected_commit_id 原样传到 native RPC；不得在 adapter 层重建 scope 或添加第二终态。</action>
  <acceptance_criteria>focused Go tests 绿；所有入口单 scope；stale/missing fence 映射 409 且无 side effect。</acceptance_criteria>
  <verify><automated>cd ../cloudfile-server/fileserver &amp;&amp; go test -count=1 -run 'Test.*Operation|Test.*Fence' . &amp;&amp; cd ../../cloudfile-docker &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>Go adapter 忠实传递 native lifecycle/fence 合同。</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: 暴露 Python fenced existing-file binding</name>
  <files>../cloudfile-server/python/seaserv/api.py</files>
  <read_first>
    - AGENTS.md
    - docs/fileop-cases.json
    - ../cloudfile-server/python/seaserv/api.py
    - ../cloudfile-server/common/rpc-service.c
  </read_first>
  <action>暴露 `cf_put_file_fenced(repo_id,tmp,parent,name,user,operation_id,generation,expected_file_id,expected_commit_id)`，参数名和次序与 C RPC/fixtures 一致；只调用 existing-file update，禁止 post/create fallback，冲突保留 native version-mismatch 语义。</action>
  <acceptance_criteria>Python/C/Go/JSON 精确同名；binding 无 unfenced fallback；focused import/signature test 通过。</acceptance_criteria>
  <verify><automated>python3 -m py_compile ../cloudfile-server/python/seaserv/api.py &amp;&amp; rg -n 'cf_put_file_fenced.*expected_commit_id' ../cloudfile-server/python/seaserv/api.py &amp;&amp; python3 tools/preflight-checks.py . ..</automated></verify>
  <done>local/office 可安全复用同一 Python fenced update binding。</done>
</task>
</tasks>
<threat_model>
| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| adapter 重建 operation identity | Spoofing | high | mitigate | stable scope Go tests |
| binding 绕过 fence | Tampering | high | mitigate | exact signature and no fallback |
</threat_model>
<verification>focused Go/Python checks+preflight；完整 fileop matrix 由 01-08。</verification>
<success_criteria>Go/Python adapter 与 native contract 无字段或终态漂移。</success_criteria>
<output>After completion, create `.planning/phases/01-security-and-collaboration-contract-closure/01-17-SUMMARY.md`</output>
